import React from 'react';
import ReactDOM from 'react-dom/client';
import StudyForge from './StudyForge';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<StudyForge />);